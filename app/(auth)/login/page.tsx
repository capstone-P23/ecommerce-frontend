import { AuthShell } from '@/components/layout/auth-shell';

// [MEM-001] 기본 회원가입/로그인 — 로그인 페이지
export default function LoginPage() {
  return (
    <AuthShell title="로그인">
      <p className="text-sm text-muted-foreground">
        TODO (Phase 4): 이메일/비밀번호 폼 + 소셜 로그인 버튼 [MEM-002]
      </p>
    </AuthShell>
  );
}
